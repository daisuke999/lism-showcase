# テンプレ配布用：ページを1つの Astro ファイルにマージする手順

このドキュメントは、**開発用の分割構成**（`page00x.astro` + `src/components/page00x/**`）を維持したまま、**配布用に1ファイルへまとめた Astro** を別パスに生成するときの指示です。  
Cursor 等のエージェントにマージ作業を依頼する際は、**このファイルを読むよう明示**したうえで作業してください。

---

## 1. 目的

- 完成した `page001` などを、**多くのユーザーがコピーして使えるテンプレート**として配布する。
- 配布形態として **「1つの `.astro` ファイルで完結」** することが必要である。
- **マージ元のページとコンポーネントは開発資産として残す**（上書き・削除しない）。

---

## 2. 用語

| 用語 | 意味 |
|------|------|
| **マージ元** | `src/pages/page00x.astro` および、そこから import している `src/components/page00x/**/*.astro`（必要なら同階層の CSS 等） |
| **マージ成果物** | マージ元の内容を1ファイルに集約した `.astro`。本リポジトリでは **`src/pages/merged/`** 配下に置く（後述） |

---

## 3. 不変条件（必ず守る）

1. **マージ元のファイルを編集しない**  
   - `src/pages/page00x.astro`  
   - `src/components/page00x/**`  
   - マージのための変更（import 削除、中身の移動など）は **マージ成果物側のみ** で行う。

2. **マージ成果物は別パスに新規作成（または該当ファイルのみ差し替え）**  
   - マージ元と同じパスに「置き換え保存」しない。

3. **成果物の責務**  
   - ページとして動作する1ファイルに、元ページ相当のマークアップ・スクリプト・スタイルが含まれること。  
   - 外部パッケージ（`lism-css` / `@lism-css/ui` / レイアウト等）の import は、**不要にならない限り維持**する。

---

## 4. 出力先の規約（本プロジェクト）

| 項目 | 値 |
|------|-----|
| ディレクトリ | `src/pages/merged/` |
| ファイル名 | マージ元に対応する名前。例：`page001.astro` → **`src/pages/merged/page001.astro`** |

- 初回は `merged` ディレクトリが無ければ作成してよい。
- `page004` 以降も同じ規約で **`merged/page00x.astro`** とする。

### 本リポジトリ: page001 の再生成

- マージ元（`src/pages/page001.astro` と `src/components/page001/**`）を更新したあと、成果物を作り直す場合は、**再生成は `scripts/emit-merged-page001.mjs`** を使う。  
  - プロジェクトルートで **`node scripts/emit-merged-page001.mjs`** を実行すると、`src/pages/merged/page001.astro` が上書きされる（スクリプトはマージ元を読み取るのみで、**マージ元ファイルは編集しない**）。

---

## 5. マージ作業で行うこと（エージェント向け）

### マージ成果物の生成方法

マージは次のいずれかで行うこと。

- **手書き** … マークアップ・frontmatter・スタイルをエディタ上で1ファイルに組み立てる。
- **スクリプト** … 複数ファイルを連結する際は、**複数行にまたがる `import` を正しく結合できる**実装にすること（行単位の机上連結は、`import {` の途中で途切れるなど**構文エラー**になりやすい）。

いずれにせよ、成果物の frontmatter は **有効な Astro / TypeScript として解釈できる**状態にする。

1. **マージ元の特定**  
   - 依頼で指定された `page00x` について、`src/pages/page00x.astro` の frontmatter の import 一覧を把握する。  
   - `@/components/page00x/` 以下の各 `.astro` を、**依存順を壊さないよう**読み込む（子コンポーネントが更に別ファイルを import している場合は再帰的に辿る）。

2. **単一ファイルへの集約**  
   - 各コンポーネントの **テンプレート部分** を、親子の構造が崩れないように **1つのファイル内**へ移す。  
   - コンポーネント専用だった `---` 内の import / 型 / 定数 / `define:vars` 等は、**重複・衝突を解消**しつち上段の frontmatter に整理する。  
   - コンポーネント名のカスタム要素（例：`<Header />`）は、**インライン化後は不要**ならそのままのタグ構造に展開してよい（可読性と重複のバランスは依頼者の好みに合わせる。原則「1ファイルで完結」が最優先）。

3. **import の整理**  
   - 同一モジュールからの import は **まとめてよい**（例：`lism-css/astro` の複数コンポーネント）。複数行の `import { ... } from '…'` は **結合後も1つの import 文として閉じる**こと（上記「生成方法」を参照）。  
   - マージにより不要になった `@/components/page00x/...` の import は **成果物から削除**する。  
   - **`DemoLayout` / `lism-css/astro` / `@lism-css/ui/astro`** は **削除せず残す**（詳細は **セクション6**）。  
   - `@/` エイリアスや `lism-css` のパスは、プロジェクトの既存の書き方に合わせる。

4. **`<style>`**  
   - 元ページ・各コンポーネントに複数の `<style>` がある場合は、**セレクタの衝突に注意**しつつ、**1ファイル内の `<style>` に統合**してよい。  
   - **`@layer lism-custom` に置かれている CSS は、成果物では1つの `@layer lism-custom { ... }` にまとめる**（マージ元が複数コンポーネント・複数 `<style>` に分かれていても、同一レイヤーへ統合する）。  
   - その他の `@layer` やスコープの意図があれば可能な限り維持する。

5. **ルーティング**  
   - `src/pages/merged/page001.astro` は **別 URL** になる（例：`/merged/page001`）。  
   - マージ作業では **既存の `page001` のルートを変えない**（マージ元の `src/pages/page001.astro` は触らないため）。

---

## 6. import の扱い（固定方針）

マージ成果物では、次の **外部 import はそのまま残す**（マージ元と同様の import 文でよい）。配布先でも、プロジェクトがこれらの依存を持てば **そのまま動作する**前提とする。

| 残す import の例 | 備考 |
|------------------|------|
| `DemoLayout`（`@/layouts/DemoLayout.astro` 等） | レイアウトはファイル分割のまま |
| `lism-css/astro` | Lism のプリミティブ・`Lism` 等 |
| `@lism-css/ui/astro` | UI コンポーネント（Button 等） |

**インライン化する対象**は、主に **`@/components/page00x/**` から import しているページ専用コンポーネント**である。これらを1ファイル内に展開し、当該 `@/components/page00x/...` の import は成果物から削除する。

上記以外の import（プロジェクト独自の `layouts` / `utils` 等）がある場合は、マージ元に合わせて残すか依頼者に確認する。

---

## 7. 完了前チェックリスト

- [ ] マージ元の `src/pages/page00x.astro` と `src/components/page00x/**` に **差分が無い**（編集していない）。  
- [ ] `src/pages/merged/page00x.astro` が存在し、ビルド可能な構文になっている。  
- [ ] 未使用の import・未使用の変数が可能な範囲で整理されている。  
- [ ] **セクション6**どおり、`DemoLayout` / `lism-css/astro` / `@lism-css/ui/astro` は import のまま残し、ページ専用コンポーネントはインライン化している。  
- [ ] **`@layer lism-custom` のスタイルが1つの `@layer lism-custom` にまとまっている**（重複レイヤーブロックになっていない）。  
- [ ] frontmatter の **複数行 `import` が壊れていない**（必要なら手書き or 正しい連結スクリプトで生成したことを確認済み）。

推奨：作業後に **`npm run build`** を実行し、エラーがないことを確認する（実行可能なら）。

---

## 8. 依頼時のチャット例

```text
docs/merge-single-file-template-pages.md の内容に従い、page001 を1つの Astro ファイルにまとめた成果物を
src/pages/merged/page001.astro として作成してください。マージ元のファイルは編集しないでください。
```

`page004` などは `page001` を読み替える。

---

## 9. メンテナンス

- プロジェクトのパス規約や import 方針が変わったら、**このファイルの「4. 出力先」「5. マージ作業」**を更新する。  
- 配布パッケージの置き場所（npm / 別リポジトリ等）が決まったら、「成果物をどこへコピーするか」を追記するとよい。

---

## 改訂履歴

| 日付 | 内容 |
|------|------|
| 2026-04-28 | セクション4に page001 再生成手順（`scripts/emit-merged-page001.mjs`）を追記 |
| 2026-04-28 | マージ生成方法（手書きまたは複数行 import に対応したスクリプト）、`@layer lism-custom` の1本化、`@layer` 追記時のチェックリストを追加 |
| 2026-04-28 | 初版。セクション6で DemoLayout / `lism-css/astro` / `@lism-css/ui/astro` の import 維持を明文化。セクション5・7を整合 |
